from web_scraper import WebScraper

def main():
    '''
    function should:
        grab all tournament urls from liquidpedia tier 1, tier 2, and tier 3
        pages
        empty this list of all tournaments whose slugs are already present in
        the database/csv
        grab the start.gg slug of all tournaments remaining in the list
        return confirmation all slugs are found and added to a list/csv/database
        create a list/csv of all tournaments with no slugs requiring manual work
    '''
    pass

if __name__ == "__main__":
    main()